/** @type {import('next').NextConfig} */
const nextConfig = {
    // ENABLE WHILE EXPORTING
    // output: "export",
    // reactStrictMode: true,
    // distDir: 'docs'
};

export default nextConfig;
